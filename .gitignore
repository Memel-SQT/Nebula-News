node_modules
.next
out
.env
.env.local
*.tsbuildinfo
.DS_Store
npm-debug.log*

# Local/dev SQLite databases and desktop build output — never committed,
# regenerated by `prisma migrate` / `npm run seed` / `npm run build:desktop`.
prisma/*.db
prisma/*.db-journal
dist-desktop/
